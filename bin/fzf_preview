#!/usr/bin/env sh

if [ -d "$1" ]; then
    exec ls -lA --color=always "$1"
elif file "$1" | grep text >/dev/null 2>/dev/null; then
    exec bat --style=numbers --color=always --line-range :300 "$1"
else
    exec mediainfo "$1"
fi
